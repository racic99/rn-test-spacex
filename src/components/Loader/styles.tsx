import { StyleSheet } from "react-native";

export default StyleSheet.create({
  containerFullHeight: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  containerSmall: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingVertical: 16,
  }
});